# Dependências da carga Postgres na VPS (Fase 4 — método → fms_prod).
# Apenas o necessário para compute_channels + step2/step3 + carga_postgres.
numpy>=1.26
pandas>=2.1
psycopg2-binary>=2.9

# Download do CSV do Drive (usado pelo cron_carga.sh).
gdown>=5

# Boletim DOCX semanal (steps 5/6 do pipeline.py) — gerado na VPS às segundas (passo 5d
# do cron_carga.sh). Após adicionar/mudar estas linhas: rebuild manual da imagem
# (docker build -t canal-carga:latest .) — o cron só rebuilda se a imagem estiver AUSENTE.
matplotlib>=3.8
python-docx>=1.1
